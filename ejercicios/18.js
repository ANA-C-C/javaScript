function esImpar(num) {
  // Retorna true si "num" es impar.
  // De lo contrario, retorna false.
  // Tu código:
  if (num % 3 === 1) {
    return true;
  } else {
    return false;
  }
}

module.exports = esImpar;